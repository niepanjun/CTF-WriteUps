我在两次比赛中都因为这片领域没有详细了解过从而连简单题都做不出来，遂痛定思痛作此篇
这类题目在CTF中通常不属于传统二进制Pwn，而是对Linux环境、Shell特性、系统工具链的熟悉程度

###步骤 1：侦察限制类型
echo $SHELL; echo $PATH; type cat; which cat 2>/dev/null
以下是我在linux试过的结果：
[Niemou@webminal ~]$ echo $SHELL                                                    
/bin/bash                                                                           
[Niemou@webminal ~]$ echo $PATH                                                     
/common_pool/shared/udocker-1.3.16/udocker:/common_pool/shared/udocker-shared/bin:/u
sr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/home/Niemou/.local/bin:/home/N
iemou/bin                                                                           
[Niemou@webminal ~]$ type cat                                                       
cat is /bin/cat                                                                     
[Niemou@webminal ~]$ echo $PATH;which cat 2>/dev/null                               
/common_pool/shared/udocker-1.3.16/udocker:/common_pool/shared/udocker-shared/bin:/u
sr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/home/Niemou/.local/bin:/home/N
iemou/bin                                                                           
/bin/cat    

通俗易懂的解释就是Linux像是一家商场，而你想要买杯奶茶，cat就是可以买到奶茶的奶茶店，而$PATH就是明确写了店铺位置的清单，可以告诉系统（保安）客人应该怎样找到奶茶店。

###Linux系统的固定规则：只要不是以" ./ "或“ / ”类形式存在的指令，都会直接放在PATH什么的里面搜索，这是默认的
例：cat flag 
它既不是“./cat"相对路径，也不是"/bin/cat"这类绝对路径，于是放在PATH里面搜索

###为什么要看$PATH?
因为我需要看PATH是否被篡改，这代表两种不同解决方式：
如果没有被篡改，清单显示中有完整的/bin,/usr/bin这些目录，那么系统就一定能找到目录，但有可能找到了但用不了（敲了cat flag依旧报错）
       用不了的情况是出现：blocked / invalid / 无输出，虽然/bin正常，PATH完好，但被出题者整了类似加密的东西（别名/函数/脚本把它“调包”或“拦截”了）
如果被篡改了（清单里面都是错误的路径，完全没有/bin，这根本不是去奶茶店的路）
       就需要知道cat的绝对路径（比如/bin/cat flag，跳过查看清单这一步，直接去奶茶店）

###常用方法：
ls /bin/cat
/bin/cat：是cat在Linux里面的默认绝对路径
作用：验证cat到底在不在，大多数题都只是修改了环境变量或加了别名，虽然敲cat报错但绝对路径还是在的，如果ls可以正常列出它，那么就代表可以直接用绝对路径/bin/cat获得flag

###Bath详细介绍：
就像餐厅里面的服务员，是一种筛选，传递的媒介。
可以接收输入命令并解析，查菜单（PATH），传入标准订单给后厨（系统调用），把菜端给用户（显示结果）

###Linux内核介绍：
就像后厨，接收标准订单（系统调用），操作设备（硬件），返回结果给服务员（Bath）

###在知道出题人用别名/函数/脚本把它“调包”或“拦截”之后应该怎么做？
敲下 cat flag 时，Bash 会按以下严格顺序决定执行什么：


cat flag 2>&1
cat flag:故意触发一次读flag操作，即使可能被拦截
2代表错误流，1代表正常输出流，>&代表把2重定向到1，也就是打印所有信息（包括正确和错误信息）从而了解出题者是怎么限制的

