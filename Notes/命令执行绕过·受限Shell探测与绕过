我在两次比赛中都因为这片领域没有详细了解过从而连简单题都做不出来，遂痛定思痛作此篇
这类题目在CTF中通常不属于传统二进制Pwn，而是对Linux环境、Shell特性、系统工具链的熟悉程度

###步骤 1：侦察限制类型
echo $SHELL; echo $PATH; type cat; which cat 2>/dev/null
以下是我在linux试过的结果：
[Niemou@webminal ~]$ echo $SHELL                                                    
/bin/bash                                                                           
[Niemou@webminal ~]$ echo $PATH                                                     
/common_pool/shared/udocker-1.3.16/udocker:/common_pool/shared/udocker-shared/bin:/u
sr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/home/Niemou/.local/bin:/home/N
iemou/bin                                                                           
[Niemou@webminal ~]$ type cat                                                       
cat is /bin/cat                                                                     
[Niemou@webminal ~]$ echo $PATH;which cat 2>/dev/null                               
/common_pool/shared/udocker-1.3.16/udocker:/common_pool/shared/udocker-shared/bin:/u
sr/local/bin:/bin:/usr/bin:/usr/local/sbin:/usr/sbin:/home/Niemou/.local/bin:/home/N
iemou/bin                                                                           
/bin/cat    

*详细解释助理解*
Linux 系统就是一个“大商场”
想象你（用户）站在商场大厅，你想买一杯奶茶（执行 cat 命令）。
1. cat 是什么？
cat 就是那家卖奶茶的店。你的目的是进店买奶茶（读文件）。
2. $PATH 是什么？
$PATH 就是商场发给你的“店铺索引清单”。
它告诉商场保安（系统）：“如果客人要找‘奶茶店’，请按这个顺序去这些楼层找：”

*看 $PATH 的目的是什么？*
情况 A（PATH 被篡改）：清单里全是垃圾目录，根本没有 /bin。
    结果：系统找了一圈没找到，报错 command not found。
    对策：你需要告诉系统 cat 的绝对地址，比如直接喊 /bin/cat flag（跳过查清单，直接去店里）。
情况 B（你的情况）：清单里明明有 /bin、/usr/bin 这些标准目录。
    结果：系统一定能找到 cat。
    结论：如果你这时候敲 cat flag 还是报错，那就绝对不是“找不到”的问题，而是“找到了但不让你用”

*核心符号速查表*
$          变量名  获取环境变量的值
;          命令分隔符，顺序执行
>          重定向（覆盖写入）
2          标准错误输出流  “1正常，2报错”
1          标准输出流 
/dev/null  数据黑洞，吞掉不显示
>&         合并输出流（把错误给正常）

ls /bin/cat
ls：列出文件或目录信息（相当于 Windows 的 dir）
/bin/cat：cat 程序在 Linux 里的默认绝对路径
作用：验证“猫”到底在不在。90% 的题只是修改了环境变量或加了别名，让你敲 cat 报错，但文件 /bin/cat 其实完好无损。如果 ls 能正常列出它（显示文件大小、权限、时间），说明直接用绝对路径 /bin/cat flag 就能读 flag。

cat flag 2>&1
cat flag：故意触发一次“读 flag”操作（明知可能被拦也要试）
2>&1：2 是错误流，1 是正常输出流，>& 意思是“把 2 重定向到 1 去”
让错误信息和正常信息一起打印在屏幕上,报错内容直接告诉你出题人怎么限制的：
cat: command not found
没在 PATH 里，或命令被替换
用绝对路径 /bin/cat 或 ./cat
Permission denied
有命令，但没权限读文件
提权、换用户、或看文件是否在其他目录
flag: No such file...
flag 不叫这名字
用 ls、ls -a、find . 找真实文件名
Blocked! / Invalid command
出题人写了自定义脚本拦截
用变量拼接、通配符、编码、解释器绕过

